<%= form_for(fornecedor) do |f| %>
  <% if fornecedor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fornecedor.errors.count, "error") %> prohibited this fornecedor from being saved:</h2>

      <ul>
      <% fornecedor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

     <div class="panel-body">
        <div class="row">
            <div class="col-lg-6">
              <div class="form-group">
                <%= f.label :nome %>
                <%= f.text_field :nome,class: "form-control", placeholder: "Entre com Nome do cliente" %>
              </div>

              <div class="form-group">
                <%= f.label :endereco %>
                <%= f.text_field :endereco,class: "form-control", placeholder: "Entre com endereco do cliente" %>
              </div>

              <div class="form-group">
                <%= f.label :cpf %>
                <%= f.text_field :cpf,class: "form-control", placeholder: "Entre com cpf do cliente" %>
              </div>

              <div class="form-group">
                <%= f.label :cnpj %>
                <%= f.text_field :cnpj,class: "form-control", placeholder: "Entre com cnpj do cliente" %>
              </div>

              <div class="form-group">
                <%= f.label :tipo_conta %>
                <%= select_tag :tipo_conta, options_for_select([['Debito', 'Debito'], ['Credito', 'Credito']]) ,class: "form-control" %>

              </div>

              <div class="form-group">
                <%= f.label :conta %>
                <%= f.text_field :conta,class: "form-control", placeholder: "Entre com conta" %>
              </div>

             <div class="form-group input-group">
                <%= f.submit class: "btn btn-default"%>
        </span>
    </div>
<% end %>

        </div>
    </div>
</div>

