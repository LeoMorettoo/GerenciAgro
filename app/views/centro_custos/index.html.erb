<p id="notice"><%= notice %></p>

<h1>Centro Custos</h1>

<table class="table table-striped table-bordered table-hover">
  <thead>
    <tr>
      <th>id</th>
      <th>Descricao</th>
      <th colspan="3">Ações</th>
    </tr>
  </thead>

  <tbody>
    <% @centro_custos.each do |centro_custo| %>
      <tr>
        <td><%= centro_custo.id %></td>
        <td><%= centro_custo.descricao %></td>
        <td><%= link_to 'Show', centro_custo %></td>
        <td><%= link_to 'Edit', edit_centro_custo_path(centro_custo) %></td>
        <td><%= link_to 'Destroy', centro_custo, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Centro Custo', new_centro_custo_path, class: "btn btn-default" %>
